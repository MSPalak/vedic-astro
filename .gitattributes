# Normalize line endings: store LF in the repo, check out native locally.
* text=auto eol=lf

# Binary assets (never normalize)
*.png binary
*.jpg binary
*.ico binary
*.woff binary
*.woff2 binary
*.wasm binary
